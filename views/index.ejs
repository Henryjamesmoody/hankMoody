<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheet/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <header class="bg-emerald-800 text-3xl w-full text-center text-white p-4"><h1>NOTES</h1></header>
    <div class="main w-full min-h-screen bg-zinc-900 p-4 ">
        <div class="form p-4 bg-zinc-800 rounded-lg w-full flex">
            <form action="/create" method="post" class="w-[80%]">
                <!-- Input field for task name -->
                <input type="text" name="title" id="" placeholder="Title name" 
                       class="w-full p-2 mb-4 bg-zinc-600 text-white rounded-md focus:outline-none focus:ring-2 focus:ring-zinc-400" required>
                
                <!-- Textarea for task description -->
                <textarea name="details" id="task_description" placeholder="Notes "
                          class="w-full min-h-[80dvh] p-2 mb-4 bg-zinc-600 text-white rounded-md resize-none focus:outline-none focus:ring-2 focus:ring-zinc-400" required></textarea>
                
                <!-- Submit button -->
                <input type="submit" value="Create Notes" 
                       class="w-full p-2 bg-emerald-600 text-white rounded-md cursor-pointer hover:bg-zinc-400 transition duration-200">
            </form>
            <div class="p-4 bg-zinc-800 rounded-lg w-[20%]">
                <textarea class="w-full min-h-[90dvh] p-2 mb-4 bg-zinc-600 text-white rounded-md resize-none focus:outline-none focus:ring-2 focus:ring-zinc-400" id="">rough work</textarea>
            </div>
        </div>

        <div class="tasks flex p-10 flex-wrap gap-4">
            <% if(files.length>0){ %>
            <% files.forEach((val)=>{ %>
                <div class="task w-72 px-3 py-4 rounded bg-zinc-700">
                    <h1 class="text-white text-2xl tracking-tighter"><%= val %></h1>
                    <a class="text-blue-500 inline-block mt-3" onclick= getData(); href="/files/<%= val %>">read more</a>
                    
                </div>
           <% }) %>
           <% } else {  %>
            <h1 class="text-2xl text-zinc-400">no task yet</h1>
            <% } %>
         
            
        </div>
        

    </div>
    <script src="/javascript/script.js"></script>
</body>
</html>